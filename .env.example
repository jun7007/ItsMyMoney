# Server port (default: 3000)
PORT=3000

# Bind address (0.0.0.0 allows LAN/Tailscale access)
HOST=0.0.0.0

# Optional: require X-Access-Pin header on API requests
# ACCESS_PIN=1234

# Price cache TTL in seconds
PRICE_CACHE_TTL=60
